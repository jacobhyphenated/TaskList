#These are placeholder values. Definitely change these.
db.user=task
db.pass=task

#Database configuration goes here
db.url=jdbc:postgresql://localhost:5432/tasklist
db.driver=org.postgresql.Driver
db.dialect=org.hibernate.dialect.PostgreSQLDialect
db.hibernate.showsql=true

#Use hbm2ddl to generate db schema from hibernate mappings
db.hbm2ddlPolicy=update